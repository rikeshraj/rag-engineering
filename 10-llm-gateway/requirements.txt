fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Required only for provider=anthropic
anthropic>=0.25.0

# Optional: exact token counting
tiktoken>=0.6.0

# Optional: for running tests
pytest>=7.0
httpx>=0.27.0
